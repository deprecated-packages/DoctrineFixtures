language: php

php:
  - 5.5
  - 5.6
  - hhvm

matrix:
  allow_failures:
    - php: hhvm

before_install:
  - composer self-update

instal:
  - composer install --no-interaction --prefer-source

before_script:
  # php linter
  - vendor/bin/parallel-lint -e php,phpt --exclude vendor .
  # php code sniffer
  - vendor/bin/phpcs src tests/ZenifyTests --standard=vendor/zenify/coding-standard/src/ZenifyCodingStandard/code-sniffer-ruleset.xml --ignore=bootstrap

script:
  - vendor/bin/tester -c tests/php.ini-unix tests

after_failure:
  - 'for i in $(find ./tests -name \*.actual); do echo "--- $i"; cat $i; echo; echo; done'
